class Task {
  String title;
  DateTime date;
  int timeToComplete;
  int priority;

  Task({
    required this.title,
    required this.date,
    required this.timeToComplete,
    required this.priority,
  });
}
